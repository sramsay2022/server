{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "lldb",
            "request": "launch",
            "preLaunchTask": "CMake Build",
            "program": "${workspaceFolder}/build/bin/server",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}
